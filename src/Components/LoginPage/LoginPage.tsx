import React from 'react';

export const LoginPage: React.FC = () => {
  return (
    <div>
      <button
        type="button"
        className="button is-info"
      >
        btn
      </button>
    </div>
  );
};
